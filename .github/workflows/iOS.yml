name: Build Unsigned IPA

on:
  push:
    branches: [ "iOS" ]
  pull_request:
    branches: [ "iOS" ]
  workflow_dispatch:

jobs:
  build:
    name: Build Unsigned IPA
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 'latest'

      - name: Fix Project Format
        run: |
          sed -i '' 's/objectVersion = 77/objectVersion = 56/' StikEMU.xcodeproj/project.pbxproj
          sed -i '' 's/preferredProjectObjectVersion = 77/preferredProjectObjectVersion = 56/' StikEMU.xcodeproj/project.pbxproj

      - name: Archive Build (Unsigned)
        run: |
          xcodebuild clean archive \
            -project StikEMU.xcodeproj \
            -scheme "StikEMU" \
            -configuration Release \
            -archivePath build/StikEMU.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            IPHONEOS_DEPLOYMENT_TARGET=17.0

      - name: Create IPA from Archive
        run: |
          # Copy the .app from the archive
          cp -R build/StikEMU.xcarchive/Products/Applications/StikEMU.app .

          # Optionally strip debug symbols from the executable (adjust path if needed)
          strip -S -x StikEMU.app/StikEMU || true

          # Optionally remove unwanted simulator slices (example shown for x86_64)
          APP_PATH="StikEMU.app"
          if [ -f "$APP_PATH/StikEMU" ]; then
            lipo "$APP_PATH/StikEMU" -info
            lipo -remove x86_64 "$APP_PATH/StikEMU" -o "$APP_PATH/StikEMU" || true
          fi

          # Package the app into an IPA
          mkdir -p Payload
          cp -R StikEMU.app Payload/
          zip -r StikEMU.ipa Payload
          rm -rf Payload StikEMU.app

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: StikEMU.ipa
          path: StikEMU.ipa
          retention-days: 90
